diff --git a/code/cgame/cg_draw.c b/code/cgame/cg_draw.c
index 9923a2c..11802ff 100644
--- a/code/cgame/cg_draw.c
+++ b/code/cgame/cg_draw.c
@@ -1280,6 +1280,40 @@ static float CG_DrawNeonWave(float y) {
 		CG_DrawSmallStringColor(320 - w/2, y, s, color);
 		y += SMALLCHAR_HEIGHT + 4;
 	}
+
+	// boss health bar (payload: "<wave> <ev> <bossHp> <bossMax>")
+	{
+		const char *cs = CG_ConfigString(CS_NEONWAVE);
+		int bossHp = 0, bossMax = 0;
+		if (cs && cs[0] && sscanf(cs, "%*i %*i %i %i", &bossHp, &bossMax) == 2
+			&& bossHp > 0 && bossMax > 0) {
+			vec4_t magenta = {1.0f, 0.2f, 1.0f, 1.0f};
+			vec4_t magentaDim = {0.3f, 0.05f, 0.3f, 0.8f};
+			int barW = 240, barH = 10;
+			int fill = bossHp * (barW - 4) / bossMax;
+			int bx = 320 - barW / 2;
+			Com_sprintf(s, sizeof(s), "BOSS");
+			w = CG_DrawStrlen(s) * SMALLCHAR_WIDTH;
+			CG_DrawSmallStringColor(320 - w/2, y, s, magenta);
+			y += SMALLCHAR_HEIGHT + 2;
+			CG_FillRect(bx, y, barW, barH, magentaDim);
+			CG_FillRect(bx + 2, y + 2, fill, barH - 4, magenta);
+			y += barH + 6;
+		}
+	}
+
+	// banked upgrade points hint
+	{
+		char ptsBuf[16];
+		trap_Cvar_VariableStringBuffer("g_neonwave_upgradepoints", ptsBuf, sizeof(ptsBuf));
+		if (atoi(ptsBuf) > 0) {
+			vec4_t gold = {1.0f, 0.85f, 0.2f, 1.0f};
+			Com_sprintf(s, sizeof(s), "UPGRADE POINT: %i  (upgrade hp|dmg|speed)", atoi(ptsBuf));
+			w = CG_DrawStrlen(s) * SMALLCHAR_WIDTH;
+			CG_DrawSmallStringColor(320 - w/2, y, s, gold);
+			y += SMALLCHAR_HEIGHT + 4;
+		}
+	}
 	return y;
 }
 #endif
diff --git a/code/game/g_active.c b/code/game/g_active.c
index 3e8ff38..ff127f2 100644
--- a/code/game/g_active.c
+++ b/code/game/g_active.c
@@ -973,6 +973,17 @@ void ClientThink_real( gentity_t *ent ) {
 	// set speed
 	client->ps.speed = g_speed.value;
 
+#ifdef NEONARENA_MOD
+	// NeonWave player speed upgrade: +5% per level (x10 fixed point)
+	if ( g_gametype.integer == GT_NEONWAVE
+			&& !( ent->r.svFlags & SVF_BOT )
+			&& client->pers.neonwaveSpeed > 0 ) {
+		int mul = 10 + client->pers.neonwaveSpeed;
+		if ( mul > 15 ) mul = 15;
+		client->ps.speed = client->ps.speed * mul / 10;
+	}
+#endif
+
 	if( bg_itemlist[client->ps.stats[STAT_PERSISTANT_POWERUP]].giTag == PW_SCOUT ) {
 		client->ps.speed *= 1.5;
 	}
diff --git a/code/game/g_client.c b/code/game/g_client.c
index e77096a..0be19a1 100644
--- a/code/game/g_client.c
+++ b/code/game/g_client.c
@@ -1197,6 +1197,14 @@ void ClientUserinfoChanged( int clientNum ) {
 			client->pers.maxHealth = 400;
 			client->pers.neonwaveBoss = qtrue;
 		}
+		// NeonWave player upgrades: +25 max HP per level (cap +150)
+		else if ( g_gametype.integer == GT_NEONWAVE
+				&& !( ent->r.svFlags & SVF_BOT )
+				&& client->pers.neonwaveUpHp > 0 ) {
+			int bonus = client->pers.neonwaveUpHp * 25;
+			if ( bonus > 150 ) bonus = 150;
+			client->pers.maxHealth = 100 + bonus;
+		}
 #endif
 	}
 	client->ps.stats[STAT_MAX_HEALTH] = client->pers.maxHealth;
@@ -1920,6 +1928,9 @@ void ClientSpawn(gentity_t *ent) {
 	// NeonWave boss: magenta constant glow so it's instantly recognizable
 	if ( g_gametype.integer == GT_NEONWAVE && ent->client->pers.neonwaveBoss ) {
 		ent->s.constantLight = 255 | ( 0 << 8 ) | ( 255 << 16 ) | ( 128 << 24 );
+		// boss armament: railgun with deep ammo pool
+		ent->client->ps.stats[STAT_WEAPONS] |= ( 1 << WP_RAILGUN );
+		ent->client->ps.ammo[WP_RAILGUN] = 999;
 	}
 #endif
 
diff --git a/code/game/g_cmds.c b/code/game/g_cmds.c
index fd9d170..9ea871e 100644
--- a/code/game/g_cmds.c
+++ b/code/game/g_cmds.c
@@ -1631,7 +1631,10 @@ static const char *gameNames[] = {
 	"Last Man Standing",
 	"Double Domination",
 	"Domination",
-	"Possession"
+	"Possession",
+#ifdef NEONARENA_MOD
+	"Neon Wave Survival",
+#endif
 };
 
 
@@ -2255,9 +2258,80 @@ void Cmd_GetMappage_f( gentity_t *ent ) {
 	trap_SendServerCommand( ent-g_entities, string );
 }
 
+/*
+===============
+Cmd_NeonUpgrade_f
+
+NeonWave: spend a banked upgrade point.
+Usage: upgrade hp | dmg | speed
+Levels: hp max 6, dmg max 5, speed max 5.
+===============
+*/
+#ifdef NEONARENA_MOD
+static void Cmd_NeonUpgrade_f( gentity_t *ent ) {
+	char arg[16], ptsBuf[16];
+	int pts, *level, cap;
+	const char *what;
+
+	if ( g_gametype.integer != GT_NEONWAVE ) {
+		trap_SendServerCommand( ent - g_entities, "print \"Upgrades only in NeonWave\n\"" );
+		return;
+	}
+	if ( ent->r.svFlags & SVF_BOT ) {
+		return;
+	}
+
+	trap_Cvar_VariableStringBuffer( "g_neonwave_upgradepoints", ptsBuf, sizeof(ptsBuf) );
+	pts = atoi( ptsBuf );
+	if ( pts < 1 ) {
+		trap_SendServerCommand( ent - g_entities,
+			va("print \"No upgrade points banked (clear waves to earn them)\n\"") );
+		return;
+	}
+
+	trap_Argv( 1, arg, sizeof(arg) );
+	if ( !Q_stricmp( arg, "hp" ) ) {
+		level = &ent->client->pers.neonwaveUpHp; cap = 6; what = "+25 max HP";
+	} else if ( !Q_stricmp( arg, "dmg" ) ) {
+		level = &ent->client->pers.neonwaveDmg; cap = 5; what = "+10% damage";
+	} else if ( !Q_stricmp( arg, "speed" ) ) {
+		level = &ent->client->pers.neonwaveSpeed; cap = 5; what = "+5% speed";
+	} else {
+		trap_SendServerCommand( ent - g_entities,
+			"print \"usage: upgrade hp|dmg|speed\n\"" );
+		return;
+	}
+
+	if ( *level >= cap ) {
+		trap_SendServerCommand( ent - g_entities,
+			va("print \"%s already at max level %i\n\"", arg, cap) );
+		return;
+	}
+	(*level)++;
+	pts--;
+	trap_Cvar_Set( "g_neonwave_upgradepoints", va("%i", pts) );
+
+	// apply immediately: raise max + heal on HP upgrade
+	if ( level == &ent->client->pers.neonwaveUpHp ) {
+		ent->client->pers.maxHealth += 25;
+		ent->client->ps.stats[STAT_MAX_HEALTH] = ent->client->pers.maxHealth;
+		ent->health += 25;
+		ent->client->ps.stats[STAT_HEALTH] = ent->health;
+	}
+
+	trap_SendServerCommand( ent - g_entities,
+		va("print \"UPGRADE: %s -> level %i (%i points left)\n\"", what, *level, pts) );
+	G_Printf( "NeonWave: %s upgraded %s to level %i\n",
+		ent->client->pers.netname, arg, *level );
+}
+#endif
+
 //KK-OAX This is the table that ClientCommands runs the console entry against. 
-commands_t cmds[ ] = 
+commands_t cmds[ ] =
 {
+#ifdef NEONARENA_MOD
+	{ "upgrade", 0, Cmd_NeonUpgrade_f },
+#endif
 	// normal commands
 	{ "team", 0, Cmd_Team_f },
 	{ "vote", 0, Cmd_Vote_f },
diff --git a/code/game/g_combat.c b/code/game/g_combat.c
index 6302035..13c93ab 100644
--- a/code/game/g_combat.c
+++ b/code/game/g_combat.c
@@ -1266,6 +1266,18 @@ void G_Damage( gentity_t *targ, gentity_t *inflictor, gentity_t *attacker,
 		damage *= g_damageModifier.value;
 	}
 
+#ifdef NEONARENA_MOD
+	// NeonWave player damage upgrade: +10% per level (x10 fixed point)
+	if ( g_gametype.integer == GT_NEONWAVE
+			&& attacker->client && targ != attacker
+			&& !(attacker->r.svFlags & SVF_BOT)
+			&& attacker->client->pers.neonwaveDmg > 0 ) {
+		int mul = 10 + attacker->client->pers.neonwaveDmg;
+		if ( mul > 20 ) mul = 20;
+		damage = damage * mul / 10;
+	}
+#endif
+
 	if ( damage < 1 ) {
 		damage = 1;
 	}
diff --git a/code/game/g_local.h b/code/game/g_local.h
index 0675e55..0345895 100644
--- a/code/game/g_local.h
+++ b/code/game/g_local.h
@@ -316,6 +316,9 @@ typedef struct {
 	qboolean    cannotWin; // Set to true if the players joins a leading team or the team with the most players
 #ifdef NEONARENA_MOD
 	qboolean    neonwaveBoss; // NeonWave boss drone
+	int         neonwaveUpHp;   // +25 max HP per level
+	int         neonwaveDmg;    // damage level (+10% per level, x10 fixed point)
+	int         neonwaveSpeed;  // speed level (+5% per level, x10 fixed point)
 #endif
 } clientPersistant_t;
 
@@ -887,7 +890,7 @@ void NeonWave_Reset( void );
 void NeonWave_StartWave( int num );
 int  NeonWave_GetWave( void );
 void NeonWave_Frame( void );
-void NeonWave_DropReward( int clearedWave );
+void NeonWave_DropReward( int clearedWave ); 
 void CheckTeamVote( int team );
 
 //
@@ -1043,10 +1046,13 @@ extern	gentity_t		g_entities[MAX_GENTITIES];
 
 #define	FOFS(x) ((size_t)&(((gentity_t *)0)->x))
 
-//CVARS 
+//CVARS
 extern vmCvar_t g_gametype;
 extern vmCvar_t g_dedicated;
 extern vmCvar_t g_cheats;
+#ifdef NEONARENA_MOD
+extern vmCvar_t g_neonwaveBest;
+#endif
 extern vmCvar_t g_maxclients;			// allow this many total, including spectators
 extern vmCvar_t g_maxGameClients;		// allow this many active
 extern vmCvar_t g_restarted;
diff --git a/code/game/g_neonwave.c b/code/game/g_neonwave.c
index d0ef8b2..4f0b375 100644
--- a/code/game/g_neonwave.c
+++ b/code/game/g_neonwave.c
@@ -7,7 +7,7 @@
 #define NW_MAX_WAVE			20
 #define NW_BOSS_WAVE		10	// from here on, each wave gets one boss drone
 
-// CS_NEONWAVE payload: "<wave> <event> <bossHp> <bossMax>"
+// CS_NEONWAVE payload: "<wave> <event>"
 // event: 0 = wave running, 1 = wave just cleared
 #define NW_EV_RUNNING		0
 #define NW_EV_CLEARED		1
@@ -45,43 +45,6 @@ static void NW_SpawnBoss( void ) {
 		va("addbot sarge 5 \"BOSS W%d\"\n", nw_wave) );
 }
 
-/*
-================
-NW_BossHealthPayload
-
-Find the live boss and append its health to the configstring payload.
-================
-*/
-static void NW_BossHealthPayload( char *buf, int size ) {
-	gentity_t *ent;
-	int i;
-
-	buf[0] = '\0';
-	for ( i = 0; i < level.maxclients; i++ ) {
-		ent = &g_entities[i];
-		if ( !ent->inuse || !ent->client ) continue;
-		if ( !( ent->r.svFlags & SVF_BOT ) ) continue;
-		if ( ent->health <= 0 ) continue;
-		if ( !ent->client->pers.neonwaveBoss ) continue;
-		Com_sprintf( buf, size, " %i %i", ent->health,
-			ent->client->ps.stats[STAT_MAX_HEALTH] );
-		return;
-	}
-}
-
-/*
-================
-NW_SendStatus
-
-Push "<wave> <event><bossHealth>" onto the NEONWAVE configstring.
-================
-*/
-static void NW_SendStatus( int event ) {
-	char bossPart[32];
-	NW_BossHealthPayload( bossPart, sizeof(bossPart) );
-	trap_SetConfigstring( CS_NEONWAVE, va( "%i %i%s", nw_wave, event, bossPart ) );
-}
-
 /*
 ================
 NeonWave_DropReward
@@ -143,7 +106,7 @@ void NeonWave_StartWave( int num ) {
 	if ( skill > 5 ) skill = 5;
 	nw_wave = num;
 	nw_botCounter = 0;
-	NW_SendStatus( NW_EV_RUNNING );
+	trap_SetConfigstring( CS_NEONWAVE, va( "%i %i", num, NW_EV_RUNNING ) );
 	G_Printf( "NeonWave: starting wave %i (%i bots, skill %i)%s\n", num, num + 1, skill,
 		num >= NW_BOSS_WAVE ? " + BOSS" : "" );
 	if ( num >= NW_BOSS_WAVE ) {
@@ -152,6 +115,7 @@ void NeonWave_StartWave( int num ) {
 	for ( i = 0; i <= num && i < MAX_CLIENTS; i++ ) {
 		NW_SpawnBot( skill );
 	}
+	nw_aliveBots += num + 1;
 }
 
 int NeonWave_GetWave( void ) {
@@ -197,20 +161,9 @@ void NeonWave_Frame( void ) {
 
 	// wave cleared -> next wave after break
 	if ( nw_aliveBots == 0 && level.time > nw_nextSpawnTime ) {
-		// announce clear event (cgame plays jingle) + grant an upgrade point
-		NW_SendStatus( NW_EV_CLEARED );
+		// announce clear event (cgame plays jingle)
+		trap_SetConfigstring( CS_NEONWAVE, va( "%i %i", nw_wave, NW_EV_CLEARED ) );
 		NeonWave_DropReward( nw_wave );
-
-		{
-			char ptsBuf[16];
-			int pts;
-			trap_Cvar_VariableStringBuffer( "g_neonwave_upgradepoints", ptsBuf, sizeof(ptsBuf) );
-			pts = atoi(ptsBuf);
-			pts += ( nw_wave >= NW_BOSS_WAVE ? 2 : 1 ); // boss waves grant 2
-			trap_Cvar_Set( "g_neonwave_upgradepoints", va("%i", pts) );
-			G_Printf( "NeonWave: upgrade point granted (%i banked)\n", pts );
-		}
-
 		if ( nw_wave >= NW_MAX_WAVE ) {
 			G_Printf( "NeonWave: all %i waves survived!\n", NW_MAX_WAVE );
 			NeonWave_UpdateHighscore();
@@ -220,12 +173,5 @@ void NeonWave_Frame( void ) {
 		}
 		NeonWave_StartWave( nw_wave + 1 );
 		nw_nextSpawnTime = level.time + NW_WAVE_BREAK;
-	} else if ( nw_aliveBots > 0 && nw_wave >= NW_BOSS_WAVE ) {
-		// refresh boss health in the status string while a boss is alive
-		static int lastRefresh = 0;
-		if ( level.time > lastRefresh ) {
-			lastRefresh = level.time + 250;
-			NW_SendStatus( NW_EV_RUNNING );
-		}
 	}
 }
